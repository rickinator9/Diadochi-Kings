vassal_decisions = {	
	enslave = {
		from_potential = {			
			is_ruler = yes
			prisoner = no
			age = 10
			NOT = { # Zoroastrianism forbids slavery
				religion = zoroastrian_main 
				religion = zurvanism
			}
		}
		potential = {
			prisoner = yes
		}
		allow = {
		}
		
		effect = {
			custom_tooltip = {
				hidden_tooltip = {
					character_event = {
						id = slavery.1
					}
				}
			}
			prisoner = no
		}
	}
	
	free_slave = {
		from_potential = {
			is_ruler = yes
			prisoner = no
			age = 10
		}
		potential = {
			trait = slave
		}
		allow = {
		}
		effect = {
			custom_tooltip = {
				hidden_tooltip = {
					character_event = {
						id = slavery.3
					}
				}
			}
		}
	}
	
	sell_slave = {
		from_potential = {
			is_ruler = yes
			prisoner = no
			age = 10
		}
		potential = {
			trait = slave
		}
		allow = {
		}
		effect = {
			set_character_flag = slave_to_be_sold
		}
	}
}

decisions = {
	buy_slaves = {
		potential = {
			is_ruler = yes
			prisoner = no
			age = 10
		}
		allow = {
		}
		effect = {
			set_character_flag = buying_slaves
		}
	}
	stop_buying_slaves = {
		potential = {
			has_character_flag = buying_slaves
		}
		allow = {
		}
		effect = {
			clr_character_flag = buying_slaves
		}
	}
}